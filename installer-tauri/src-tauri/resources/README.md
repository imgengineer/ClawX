`payload.zip` is generated by `pnpm run build:installer`.

The tiny placeholder archive committed alongside this file only keeps Tauri builds and
`cargo check` working in development. Real installer builds overwrite it with the
platform-specific unpacked Electron payload.
